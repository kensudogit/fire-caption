/**
 * 地図表示コンポーネント
 *
 * 消防司令システムの地図表示機能を提供します。
 * 消防署の位置、緊急通報の位置、出動ルートなどの地図表示を担当します。
 *
 * @author FireCaptain Team
 * @version 1.0
 */

import { Component } from '@angular/core';

@Component({
  selector: 'app-map',
  template: '<div>地図表示コンポーネント</div>'
})
export class MapComponent {
  // 地図表示の実装予定
}
